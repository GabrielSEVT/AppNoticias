<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.appnoticias">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppNoticias"
        android:usesCleartextTraffic="true">
        <activity android:name=".TelaNoticiaCompleta2"></activity>
        <activity android:name=".TelaSaibaMais2" />
        <activity android:name=".TelaNoticiaCompleta1" />
        <activity android:name=".TelaSaibaMais1" />
        <activity android:name=".TelaPerfil" />
        <activity android:name=".TelaEscolherAssunto" />
        <activity android:name=".TelaMenuPrincipal" />
        <activity android:name=".TelaRecentes" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>